@model IEnumerable<FamilyMember>

<div class="w-100 card border-0 p-4">
    <div class="card-header bg-success bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Family Members</h2>
            </div>
        </div>
    </div>
    <div class="card-body border p-4">
        <div class="row pb-3">
            <div class="col-6 offset-6 text-end">
                <a asp-controller="FamilyMember" asp-action="Create" class="btn btn-secondary">
                    <i class="bi bi-plus-circle"></i> New Family Member
                </a>
            </div>
        </div>

        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        ಹೆಸರು
                    </th>
                    
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td>@obj.FamilyMemberName</td>
                        
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="FamilyMember" asp-action="Update" asp-route-familyMemberId="@obj.FamilyMemberId"
                                   class="btn btn-success mx-2">
                                    ಮಾರ್ಪಡಿಸು
                                </a>
                                <a asp-controller="FamilyMember" asp-action="Delete" asp-route-familyMemberId="@obj.FamilyMemberId"
                                   class="btn btn-danger mx-2">
                                    ಅಳಿಸು
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>